{
  "name": "typed_cli",
  "version": "1.0.0",
  "private": true,
  "description": "CSV validation CLI with shared types and tool chaining",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "typescript": "^6.0.0",
    "tsx": "^4.7.0"
  }
}
